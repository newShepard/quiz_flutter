abstract class Recordable {
  int? id;
  Recordable({this.id});
}

abstract class Sortable {
  int? sortOtder;
  Sortable({this.sortOtder});
}

abstract class Nameable {
  String? name;
  Nameable({this.name});
}
